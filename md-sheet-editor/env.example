# MD Sheet Editor - Complete Environment Configuration
# This file contains all environment variables needed for the entire project

# =============================================================================
# N8N BACKEND CONFIGURATION
# =============================================================================

# Google Sheets OAuth2 API Credential ID from n8n
# This is the ID of your Google Sheets credential in n8n
# You can find this in n8n under Settings > Credentials
GOOGLE_SHEETS_OAUTH2_API_ID=your_credential_id_here

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# N8N Domain Configuration
# At least one of REACT_APP_N8N_LOCALHOST or REACT_APP_N8N_CUSTOM_DOMAIN must be set

# Option 1: Localhost configuration (for local development)
REACT_APP_N8N_LOCALHOST=localhost
REACT_APP_N8N_PORT=5678

# Option 2: Custom domain configuration (for production)
# REACT_APP_N8N_CUSTOM_DOMAIN=your-n8n-domain.com

# Webhook names (optional - defaults shown)
# These should match the webhook names that you want in your n8n workflows and is not already used somewhere else
REACT_APP_N8N_FETCH_WEBHOOK=Fetch-Rows-Multi
REACT_APP_N8N_UPDATE_WEBHOOK=Update-Row-Multi

# =============================================================================
# DOCUMENT AND SHEET CONFIGURATION
# =============================================================================

# Document and Sheet Configuration with Matching Columns
# Format: "doc1:sheet1[matchCol1],sheet2[matchCol2];doc2:sheet3[matchCol3]"
# Each document is separated by semicolon (;)
# For each document, sheets are separated by comma (,)
# [matchingColumn] is required for proper row updates (matches existing rows instead of always appending)
# Example: "Links:Meeting[Meeting Name],mixed[Link];Projects:Tasks[Task Name],Notes[Title]"
REACT_APP_DOC_SHEET_CONFIG="My Document:Sheet1[ID],Sheet2[Name];Another Doc:Sheet3[Email]"

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Local development only:
# REACT_APP_N8N_LOCALHOST=localhost
# REACT_APP_N8N_PORT=5678

# Production only:
# REACT_APP_N8N_CUSTOM_DOMAIN=n8n.yourdomain.com

# Both localhost and production (will try localhost first, then production):
# REACT_APP_N8N_LOCALHOST=localhost
# REACT_APP_N8N_PORT=5678
# REACT_APP_N8N_CUSTOM_DOMAIN=n8n.yourdomain.com

# =============================================================================
# OPTIONAL: CUSTOM WEBHOOK PATHS (for template generation)
# =============================================================================

# Optional: Custom webhook paths (defaults will be used if not specified)
# These are used by the n8n template generator
# FETCH_WEBHOOK_PATH=Fetch-Rows-Multi
# UPDATE_WEBHOOK_PATH=Update-Row-Multi 